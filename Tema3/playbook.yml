---
- hosts: all
  become: yes
  gather_facts: False
  tasks:
    - name: Install base packages
      apt: name={{ item }} state=present
      with_items:
        - git
        - python-pip
        - python3-pip
        - python-setuptools
      tags:
        - packages

- import_playbook: specific_playbook.yml
